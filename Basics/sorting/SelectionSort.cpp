#include <iostream>
using namespace std;

// Selection Sort Algorithm
// The idea is to divide the array into two parts: sorted and unsorted.
// Repeatedly select the smallest (or largest, depending on sorting order) element from the
// unsorted part and move it to the start index of the sorted part.


// by default we take whole array as unsorted